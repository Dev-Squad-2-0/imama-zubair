# Week 3 Day 3: SQL Aggregation, Subqueries and CTEs

## Overview

This one builds on SQL JOINs by introducing aggregation, subqueries, Common Table Expressions (CTEs), and window functions. The goal is to answer more complex business questions by summarizing data, breaking queries into smaller steps, and ranking results.

---

## Dataset

This project uses the PostgreSQL DVD Rental sample database. The same as before

**Source:**: https://neon.com/postgresql/getting-started/sample-database

---



## When to Use Each SQL Feature

### Subquery

Use a subquery when another query depends on the result of a smaller query.

Example: Customers spending above the average customer.

### CTE

Use a CTE when a query has multiple logical steps and readability is important.

Example: Ranking customers after calculating their total spending.

### Window Function

Use window functions when calculations should keep every row instead of grouping everything into one row.

Example:
1. Ranking customers within each city.
2. Finding the latest rental for every customer.

**Note**: use a subquery for a one-off value, a CTE to organize a multi-step query, and a window function whenever the output needs to stay row-level while still comparing against a group.

---

## Business Questions

### Part 1: Aggregation Basics

#### 1. Calculate total revenue generated by each store.

**Explanation:** payment -> staff -> store. Grouped payment by store (via staff) and summed the amount using SUM()


#### 2. Find the average rental duration for each film category.

**Explanation:** This query joins the film, film_category, and category tables. It groups films by category and uses AVG() to calculate the average rental duration for each category.


#### 3. Count rentals made each month.

**Explanation:** This query extracts the month from the rental date, groups rentals by month, and counts the total number of rentals made in each month using COUNT().


#### 4. Find categories containing more than 50 films.

**Explanation:** This query groups films vy category and counts how many films belong to each one. The HAVING clause filters grouped result to show categories that contain more than 50 films


### Part 2: Subqueries

#### 5. Find customers who spent more than the average customer.

**Explanation:** A subquery in FROM computes total spend per customer; a second subquery computes the average of those totals; the outer query compares each customer against it.



#### 6. Find the highest rental-rate film in each category.

**Explanation:** A correlated subquery checks, for each film, whether its rental_rate equals the max rate within its own category, the inner query re-runs per outer row since it references fc.category_id from the outer query


#### 7. Find customers who never rented a film.

**Explanation:** Used NOT EXISTS (safer than NOT IN when NULLs are possible) to find customers with no matching row in rental


#### 8. Find the store with the highest revenue.

**Explanation:** Computed revenue per store in a subquery, then used HAVING SUM() = (subquery for MAX) to isolate the top store.



### Part 3: CTEs & Window Functions

#### 9. Rank customers by total spending within each city.

**Explanation:** A CTE (customer_spend) computes total spend per customer per city; RANK() OVER (PARTITION BY city ORDER BY total_spent DESC) ranks them within each city without losing individual rows


#### 10. Find each customer's most recently rented film.

**Explanation:** A CTE assigns ROW_NUMBER() per customer ordered by rental_date DESC; filtering WHERE rn = 1 returns just the latest rental per customer.


#### 11. Calculate month-over-month rental revenue growth.

**Explanation:** A CTE calculates the total revenue earned each month. The LAG() window function compares each month's revenue with the previous month, allowing the revenue growth to be calculated.


#### 12. Find the top three highest-grossing films in each category.

**Explanation:** A CTE calculates the total revenue earned by each film. RANK() is then used to rank films within each category, and only the top three films from every category are returned.



#### Bonus Question: Which staff member processed the highest revenue in each store, and what percentage of that store's total revenue did they contribute?

**Explanation:** The first CTE calculates the revenue generated by each staff member. The second CTE calculates each store's total revenue. Finally, ROW_NUMBER() identifies the highest-earning staff member in each store, and their contribution percentage is calculated by dividing their revenue by the store's total revenue.

---



## Business Insights

### **Insight 1: Store Performance**

Store 2 generated slightly more revenue than Store 1, earning $31,059.92 compared to $30,252.12. This suggests that both stores performed similarly, but Store 2 had a small advantage in total sales.

### **Insight 2: Customer Spending**

Customer spending varies quite a bit. The highest-spending customer spent $211.55, while many other customers spent just over $100. This shows that a relatively small group of customers contributes significantly more revenue than the average customer.

### **Insight 3: Film Performance**

Some films earned much more revenue than others within their categories. For example, "Telegraph Voyage" was the highest-grossing Music film with $215.75, while "Zorro Ark" led the Comedy category with $199.72. These top-performing films could be promoted more often since they appear to be customer favorites.

---

## Files Included

```
aggregation_subqueries.sql
README.md
concept_check.md
screenshots/
```
---

## How to Run

1. Open pgAdmin.
2. Connect to the DVD Rental database.
3. Open `aggregation_subqueries.sql`.
4. Execute each section individually or run the entire script.

## Author

*Imama Zubair*

AI & Data Science Intern @ Netixsol